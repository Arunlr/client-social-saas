{
  "name": "client-social-saas-worker",
  "private": true,
  "scripts": {
    "check": "wrangler check",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}